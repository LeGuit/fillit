





#include <stdbool.h>

bool	find_position_for_piece()
{
  for_each(toutes_les_positions_possibles)
  {

    if (ca rentre)
      {
      //      return true_j'ai_trouve_une_position;
	bool test = recursion_totale_pour_la_piece_suivante();
	
	if (test)
	  return true;
	
      }
  }
  return false_carentrenulpart;
}



bool		procedureN(pieces, map, params...(N-1));


bool		procedure4(pieces, map, param1, param2, param3);
bool		procedure3(pieces, map, param1, param2);
bool		procedure2(pieces, map, param1);
bool		procedure1(pieces, map);

bool		point_entree_recursion(pieces, map);



void		point_entree_solveur(pieces);
